# basura del sistema
.DS_Store
Thumbs.db
desktop.ini

# dependencias
node_modules/

# vínculo local con el proyecto de Vercel (contiene los IDs de la cuenta)
.vercel/

# ajustes locales de Claude Code, propios de cada máquina
.claude/settings.local.json

# enlaces a los skills: los crea `skills add` con rutas absolutas de esta
# máquina, así que no sirven en otra. La fuente versionada va en .agents/
.claude/skills/
